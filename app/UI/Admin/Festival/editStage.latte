{block content}
<a n:href="detail , $festival->id" class="btn btn-primary">Zpět na festival</a>
<h2>Přidat kapelu</h2>

<a n:href="Bands:addBand , $festival->id, $stage->id" class="btn btn-primary">Přidat kapelu</a>
<h2>Kapely na {$stage->name}</h2>
<table class="table table-striped">
    <thead>
        <tr>
            <th>Čas</th>
            <th>Název kapely</th>
            <th>Akce</th>
        </tr>
    </thead>
    <tbody>
        {foreach $bands as $band}
            <tr>
                <td>{$band->time}</td>
                <td><b>{$band->name}</b></td>
                <td>
                    <a n:href="deleteBand! , $band->id" class="btn btn-danger btn-sm" onclick="return confirm('Opravdu chcete smazat tuto kapelu?')">Smazat</a>
                    <a n:href="Bands:editBand , $festival->id, $stage->id , $band->id " class="btn btn-primary btn-sm">Upravit</a>
                </td>
            </tr>
        {/foreach}
    </tbody>
</table>

{/block}